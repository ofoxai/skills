# Create awesome-seedance-2.5 collection repo

## Goal

Scaffold a new, separate repo (`~/code/awesome-seedance-2.5`, eventual
GitHub identity `ofoxai/awesome-seedance-2.5`) per doc §1/§3.1/§4 — a
case/prompt showcase repo distinct from this repo (`ofoxai/skills`).

**This repo's own requirements live in its own PRD**, not duplicated here:
`~/code/awesome-seedance-2.5/PRD.md`. That file is the source of truth for
implementation, acceptance criteria, and scope. This trellis task only
tracks that the work is happening and links to it, so the two projects'
planning docs don't get mixed together.

## Decisions locked in during brainstorm (2026-08-30)

- Site page `ofox.ai/seedance-2-5-prompts` not built yet → this round is
  **local-only scaffold**, no `gh repo create`, no `git push`.
- **Zero real cases** this round — structure/templates only. Real cases
  need the user's own Ofox-playground runs (prompt/params/video/bill),
  deferred.
- Repo location: `~/code/awesome-seedance-2.5` (sibling to this repo),
  git-initialized with `main` as default branch.

## Requirements / Acceptance Criteria / Out of Scope

See `~/code/awesome-seedance-2.5/PRD.md`.

## Technical Notes

- Source doc for both this task and the new repo's PRD:
  `2026-08-28_GitHub两类仓库操作手册.md` (this repo's root) §1, §2.2, §2.3,
  §3.1, §4 (all subsections), §6, §7, §9
- Pricing numbers must match `skills/ofox-video-core/references/
  pricing.md` in this repo exactly
