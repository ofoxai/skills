# Ofox GitHub 仓库项目：任务书（2026-08-28）

负责人：实习生（之前做过 design 生图工具的那位）
对接：Chris（内容/分发）、研发（API 接口和 key）
周期：第一版 2 周，之后每周迭代

---

## 1. 目的

两个，都要达到：

1. 给 ofox.ai 引流。仓库里每个案例、每个按钮都链回 ofox 站内页，带 utm 参数，后台能看到从 GitHub 来了多少人、多少人注册。
2. 拿 GitHub 外链。GitHub 域名权重 97，我们的模型页现在没有任何来自 GitHub 的链接。竞品 Evolink 在 GitHub 上有 125 条链接、28 个仓库，我们是 3 条。

顺带的：仓库被 skill 目录（clawhub、skills.sh、lobehub、npm）收录后，这些站也会给 ofox 链接，都是权重 70 到 90 的站。

不追求的：星数。有当然好，但不是验收标准。验收标准见第 7 节。

---

## 2. 参考对象

竞品 Evolink 是主要参考。它是和 ofox 同类的 API 聚合平台，GitHub 打法已经跑了半年，数据我们看得到。

### 2.1 Evolink 的仓库和它们链向的页面

Ahrefs 抓到的 Evolink 仓库共 28 个，下面是它自己的两个组织下的。"链向"一列是仓库 README 里放的回链目标，注意它链的是文档页、取 key 页和模型页，很少链首页。

| 仓库 | 回链数 | 链向 evolink.ai 的哪些页 |
|---|---|---|
| github.com/Evolink-AI/Awesome-Seed-Audio-1.0-Guide-and-Usecases | 21 | docs 音频系列、/dashboard/keys、/seed-audio-1-0（模型页） |
| github.com/Evolink-AI/Seedance-2.0-Gateway-Service | 15 | docs 首页、Seedance 2.0 文档、Nano Banana 2 文档、/dashboard/keys |
| github.com/EvoLinkAI/EvoLinkAI（组织主页 README） | 10 | 首页、/claude-opus-4-7、/grok-imagine-video、/minimax-m2-5（模型页） |
| github.com/Evolink-AI/midjourney-api | 7 | Midjourney 文档、/dashboard/keys |
| github.com/Evolink-AI/awesome-seedance-2.5-guide | 7 | Seedance 文档、/launch/seedance-2-5、/seedance-2-5-prompts、/dashboard/keys |
| github.com/Evolink-AI/GPT-Image-2-Seedance2-Workflow | 6 | docs、/gpt-image-2-prompts、/launch/seedance-2-5 |
| github.com/Evolink-AI/seed-audio-1-0-skill | 5 | Seed Audio 文档、/dashboard/keys |
| github.com/Evolink-AI/awesome-gpt-5.6-usecases | 4 | GPT-5.6 文档、/gpt-5-6（模型页） |
| github.com/Evolink-AI/openclaw-models-list | 3 | OpenClaw 集成文档、/openclaw-hosting |
| github.com/Evolink-AI/gpt-image-2-gen-skill | 3 | GPT Image 2 文档、/gpt-image-2-prompts |
| github.com/Evolink-AI/seedream-5-pro-image-generate-api-skill | 3 | Seedream 5.0 文档、/seedream-5-0（模型页） |
| github.com/EvoLinkAI/midjourney-api | 3 | Midjourney 文档、/midjourney-v7（模型页）、/signup |
| github.com/EvoLinkAI/awesome-suno-api | 3 | Suno 文档、/suno（模型页）、/signup |
| github.com/EvoLinkAI/awesome-gpt-image-2-API-and-Prompts | 3 | GPT Image 2 文档、/dashboard、/gpt-image-2-prompts |
| github.com/EvoLinkAI/evolink-media-mcp | 2 | /dashboard/keys、/models |
| github.com/EvoLinkAI/awesome-gpt-image-2-prompts | 2 | /gpt-image-2-prompts、/models |
| github.com/Evolink-AI/topaz-video-upscale-api-skill | 1 | /topaz-video-upscale（模型页） |
| github.com/EvoLinkAI/Awesome-Nano-Banana-2-prompt | 1 | /zh/nano-banana-2（模型页） |
| github.com/Evolink-AI/awesome-glm-5.2-usecases | 1 | /glm-5-2（模型页） |
| github.com/Evolink-AI/evolink-agent-skills | 1 | /launch/seedance-2-5 |

说明：Evolink-AI 这个组织下的仓库现在打开是 404，7 月建的，8 月已经改名或删了。EvoLinkAI 组织下的还在。看结构去 EvoLinkAI 那边看。

### 2.2 重点拆三个

**github.com/EvoLinkAI/awesome-gpt-image-2-API-and-Prompts**，16,985 星，1,716 fork。合集仓库的样板。README 里 599 条链接指向 evolink.ai/gpt-image-2-prompts，每个案例一条。

**github.com/freestylefly/awesome-gpt-image-2**，23,712 星。另一种合集做法，README 当首页，案例下沉到 docs/gallery.md，按分类九宫格展示。

**github.com/EvoLinkAI/gpt-image-2-gen-skill**，skill 仓库的样板。56 星，但上了 npm、clawhub、skills.sh。

**github.com/EvoLinkAI/evolink-agent-skills**，按场景拆的 skill 合集，一个仓库里 8 个 skill：ad-creative-images、ad-video-generation、image-edit、image-to-video、product-demo-video、product-image-generation、product-to-ad、ugc-video-ads。我们的场景 skill 参考这个结构。

### 2.3 从它们那里学什么、不学什么

学：每个案例一条回链、带 utm；多语言 README；npm 一行安装；装完第一句话引导注册 key；文档写给 AI agent 读（llms-install.md）。

不学：Evolink 7 月用新组织把旧仓库重发一遍凑数，现在全 404 了；它 awesome 仓库里 463 个案例只有 16 张图，大部分案例没有输出预览，我们每个案例必须有输出。

---

## 3. 要做的两类仓库

### 3.1 合集仓库：awesome-seedance-2.5

一个模型一个，先做 Seedance 2.5。目的是拿星、被转载、被 Google 收录，每个案例链回站内页。

Seedance 2.5 的 API 是 8/7 才开放的，现在 GitHub 上没有原生做 2.5 的爆款仓库，头部是几个从 2.0 改名过来的。窗口还开着，但按 Seedance 2.0 和 GPT Image 2 的经验，模型发布后头两周建的仓库拿走了几乎全部星，所以第一版要快。

### 3.2 skill 仓库：按场景分

skill 是给 Claude Code、OpenClaw、Cursor 这类 AI agent 装的插件。用户装了之后对 agent 说"给我做一条 15 秒产品广告"，agent 会调 ofox 的 API 生成。装的时候会要 ofox 的 API key，所以每一个装了的用户都是注册用户。

按使用场景分，不按模型分。一个仓库，里面每个场景一个子目录，各有自己的 SKILL.md。场景清单：

| 场景 | 目录名 | 用什么模型 | 用户会说的话 |
|---|---|---|---|
| 短剧 | short-drama | Seedance 2.5（多镜头、对白、真人感） | "按这个剧本生成第 3 场，两个角色对话，15 秒" |
| 漫剧 | anime-drama | Seedance 2.5 + Nano Banana（先出角色设定图再生视频） | "把这段小说改成动漫分镜并生成视频" |
| 创意广告 | ad-creative | Seedance 2.5 / Wan 2.7 | "给这瓶香水做一条 10 秒品牌广告，电影感" |
| 电商产品视频 | product-video | Seedance 2.5 图生视频 | "把这张产品图做成 360 度展示，白底，5 秒" |
| 口播和数字人 | talking-head | HappyHorse 1.1（多语言口型同步） | "用这张头像念这段文案，中英各一版" |
| UGC 风格广告 | ugc-ads | Seedance 2.5 | "做一条手机拍的真实感开箱视频" |
| 社媒短视频 | shorts-reels | Seedance 2.5 Fast / 2.0 Mini（便宜、快） | "做 5 条竖屏 9:16 的 Shorts 素材" |
| 产品演示 | product-demo | Seedance 2.5 首尾帧控制 | "从这个界面截图过渡到那个截图，做成演示动画" |
| 首尾帧动画 | keyframe-animation | Seedance 2.5 首尾帧 | "开头是 A 图结尾是 B 图，中间补出来" |
| 视频延长和编辑 | video-extend-edit | Seedance 2.5 视频输入 | "把这条 5 秒延长到 15 秒，保持风格" |
| 图生图和修图 | image-edit | GPT Image 2 / Nano Banana | "把这张图背景换成海边，人物不变" |
| 电商主图和海报 | product-image | GPT Image 2 / Seedream | "给这个产品出 4 张不同风格的主图" |
| 音乐可视化和 MV | music-video | Seedance 2.5 音频参考 | "按这段音乐生成配套画面" |
| 教学解说视频 | explainer | Seedance 2.5 + 配音 | "把这篇文章做成 30 秒解说视频" |

### 3.3 和已有 Seedance skill 的差异化

GitHub 上 Seedance 相关 skill 已经不少，8/28 查的情况：

| 仓库 | 星 | 建于 | 做什么 |
|---|---|---|---|
| dexhunter/seedance2-skill | 3,453 | 02-11 | 写 2.0 提示词 |
| songguoxs/seedance-prompt-skill | 2,733 | 02-12 | 写 2.0 提示词 |
| liangdabiao/Seedance2-Storyboard-Generator | 2,211 | 02-16 | 小说转多集分镜脚本 |
| LeoYeAI/seedance-skills | 大 | 07 更新 | 28 个导演类 skill，运镜、灯光、角色、多段续写 |
| nutllwhy/seedance-tvc-director | 206 | 08-09 | 2.5 广告片导演，15s/30s 脚本 |
| liyue-aigc/seedance-2-5-video-director | 119 | 08-18 | 2.5 视频导演，提示词、诊断、人物锁定、续写 |
| lixiaoxiao9888-create/manju-laoli-skill | 109 | 08-15 | 漫剧编剧和视听导演全流程 |
| huangbai-AI/sd-2-5-prompt | 100 | 08-02 | 2.5 五段式提示词 |

共同点：全部是"写提示词"的 skill。README 里没有 API 调用、没有 curl、没有 key。用法是 agent 帮你写好一段提示词，你复制去即梦网页里点生成。8/1 以后新建的 2.5 仓库也全是这个路数，两周内已经有五六个，导演和编剧这一层已经挤满了。

我们不做第九个提示词 skill。做它们都没做的那一层：

1. 执行。用户说一句话，skill 写提示词、调 ofox API、轮询、把 mp4 下到本地、报这次花了多少钱。现有 skill 停在"提示词写好了，去即梦粘贴"，我们是"视频在这，账单在这"。这是市面上零个的空位。
2. 兼容上游。不和 LeoYeAI、seedance-tvc-director 这些抢导演层，而是接它们的输出。SKILL.md 里写明：可以先用 LeoYeAI/seedance-skills 或 seedance-2-5-video-director 写提示词，再用 ofox skill 执行。同时给这几个头部仓库提 PR，在它们 README 加一节 "Run it via API（ofox）"，附三行命令。对方多一个执行渠道，我们拿链接和它们的用户。
3. 费用。生成前按参数报预估价，生成后读账单报实际价，连跑 N 条时统计"出一条可用素材花了多少"。做视频的人最痛的是抽卡成本，没有一个 skill 算这个账。
4. 多模型和回退。同一个场景 agent 可以在 Seedance 2.5、Wan 2.7、HappyHorse 之间选，便宜的先跑，审核拒了或超时自动换模型重试。单模型 skill 做不到，这是聚合平台独有的。
5. 抽卡。一次生成 N 条，自动抽每条的首帧、中帧、尾帧拼成对比图，让用户或 agent 挑。可以再接一个视觉模型按提示词打分排序。"抽卡师"是短剧团队的核心岗位，这个工具替代的是人工挑片的时间。
6. 2.5 的新能力做成可执行流程。首尾帧、视频延长、参考图生视频、多段拼 30 秒以上，现有 skill 只在提示词层面提到，我们把每一个做成一条命令跑通。
7. 面向海外用户。方舟要中国主体和企业认证，即梦要国内手机号。README 第一句写清楚"不需要中国主体，注册 30 秒"。英文、日文 README 先于中文。国内导演类 skill 的作者们也解决不了海外用户进不去的问题。

一句话：别人教你怎么写，我们帮你出片并告诉你花了多少钱。

先做前四个（短剧、漫剧、创意广告、电商产品视频），跑通后每周加两个。每个场景 skill 里要有：这个场景的提示词模板、推荐参数（时长、分辨率、宽高比）、常见失败和修法、一次生成大概多少钱。

模型对应关系以 ofox 当时在售为准，Sora、Veo、Kling 上线后补进对应场景。

---

## 4. 合集仓库操作步骤

### 4.1 前置：站内页

先让研发或 Chris 建 `ofox.ai/seedance-2-5-prompts` 这个页面。仓库里所有案例链接都指它。页面没建好之前仓库不要发。这页的内容就是仓库案例的镜像，后期可以做成自动同步。

### 4.2 组织和仓库

组织已经有了：github.com/ofoxai，12 个仓库。和这个项目有关的：

| 仓库 | 是什么 | 这次怎么用 |
|---|---|---|
| ofoxai/.github | 组织主页 README（profile/README.md），已有 Docs / Discord / X 徽章和国际站国内站表格 | 加一节 "Video & Image Models"，列 Seedance 2.5、Wan 2.7、GPT Image 2、Nano Banana 的模型页链接，再加 awesome 仓库和 skills 仓库入口。Evolink 同类页面有 10 条回链 |
| ofoxai/skills | 已有的 skill 单仓库，3 个 skill（hal-vault、hal-image、cloudflare-drop），有 CONTRIBUTING.md、skills.sh.json，`npx skills add ofoxai/skills` 可装 | 场景 skill 放进这里，见 §5.2 |
| ofoxai/works-with-ofox | 基于 ofox 构建的应用展示列表 | 合集仓库和 skill 上线后加进去互链 |
| ofoxai/ofox-cookbook | API 可运行示例 | 加视频接口的示例，链模型页 |
| ofoxai/ofox-aigc | 生图 Web UI | 实习生之前做的那个，README 里加 Seedance 视频入口和模型页链接 |

要新建的只有一个：`ofoxai/awesome-seedance-2.5`，Public，协议 CC0。

组织头像、简介、网站已经填好，不用动。

### 4.3 README 结构

按顺序：

1. 横幅图，点击进 `ofox.ai/seedance-2-5-prompts?utm_source=github&utm_medium=banner&utm_campaign=awesome-seedance-2-5`
2. 徽章一行：awesome 徽章、协议、案例数、"Try Seedance 2.5 on Ofox"（进 playground）、"Get API Key"（进取 key 页）
3. 语言切换：English / 简体中文 / 日本語，各一个 README 文件
4. 一段介绍 Seedance 2.5 是什么、能做什么，附一个 5 行的 curl 示例调 ofox 接口
5. 价格表：480p / 720p / 1080p 每秒价格，以 ofox 模型页为准，写"截至 2026-08 月"
6. 目录：按第 3.2 节的场景分类
7. 案例正文
8. How to Contribute：欢迎提 PR，格式要求
9. Related：链 ofox 其他仓库和几个第三方 awesome 列表

### 4.4 案例格式

每个案例固定这样写：

```
### Case 12: 香水品牌广告，电影感慢推镜头（by @作者 或 Ofox Team）

| 输出 |
|:---:|
| <a href="https://ofox.ai/seedance-2-5-prompts?utm_source=github&utm_medium=case&utm_campaign=awesome-seedance-2-5#case-12"><img src="images/case12/preview.gif" width="400"></a> |

**Prompt**
```
完整提示词原文，不删不改
```

**参数**：Seedance 2.5 · 文生视频 · 10s · 1080p · 16:9 · 带音频
**费用**：$4.80（10s × $0.48/s，ofox 后台账单截图见 images/case12/bill.png）
**来源**：原帖链接 / Ofox 实测
```

要求：
- 输出预览用 GIF（前 3 秒即可）或首帧截图，文件放 `images/caseN/`，不超过 2MB。视频原文件放站内页，仓库不存视频。
- 提示词必须是完整原文。没有完整提示词的作品不收。
- 费用一项只有自己跑的案例有，社区案例写"来源"。费用是我们和 Evolink 的差异点，别省。
- 社区案例必须链原作者，X 帖子或即梦作品页。作者看到会转发。

### 4.5 案例来源和比例

- 自己跑的：占一半以上。在 ofox playground 跑，记提示词、参数、账单。每个场景先跑 5 个。
- 社区的：X 上搜 "seedance 2.5" 带提示词的作品、即梦社区、Reddit r/aivideo。只收公开了完整提示词的。
- 官方的：字节 Seed 博客、火山方舟 Seedance 2.5 教程页里的示例，标来源。

### 4.6 节奏

第 1 周：站内页 + 仓库上线，30 个案例，其中 15 个自跑带账单，三语 README。
第 2 周：补到 80 个。对外推：给 `github.com/foss42/awesome-generative-ai-apis` 提 PR 加 ofox；qiita 和 zenn 用日文各发一篇 "Seedance 2.5 プロンプト集"，链仓库和站内页；Reddit r/aivideo、V2EX、Linux.do 各发一帖。
之后：每周加 10 到 20 个，处理社区 PR。

### 4.7 Claude Code 操作方法

全程在 Claude Code 里做，下面是每一步对它说什么。

1. 建仓库骨架。新建空目录，进 Claude Code，说：
   "参考 github.com/EvoLinkAI/awesome-gpt-image-2-API-and-Prompts 的 README 结构，建一个 awesome-seedance-2.5 仓库骨架。链接全部指 ofox.ai，带 utm_source=github。案例模板按下面格式（把 §4.4 的格式贴给它）。分类用这几个（把 §3.2 的视频场景贴给它）。生成 README.md、README.zh-CN.md、README.ja.md、LICENSE（CC0）、CONTRIBUTING.md、images/ 目录。"
2. 加案例。每跑完一个，把提示词、参数、输出视频文件、账单截图丢给它，说："按模板加一个案例到 X 分类，视频转成前 3 秒的 GIF 放 images/caseN/，账单图也放进去。"
3. 推仓库。说："在 ofoxai 组织下建公开仓库 awesome-seedance-2.5 并推上去。" 第一次会要求 `gh auth login`，浏览器点一下授权。
4. 改组织主页。说："clone ofoxai/.github，在 profile/README.md 加一节 Video & Image Models，列这几个模型页链接（贴链接），提 PR。"
5. 翻译。说："把 README.md 同步翻成中文和日文版，模型名、参数、价格数字不翻。" 翻完自己扫日文版的数字。
6. 对外推。说："给 github.com/foss42/awesome-generative-ai-apis 提 PR，在视频生成一节加 ofox，一行描述，链模型页。" 其余平台（qiita、zenn、Reddit）的帖子让它起草，自己发。

自己动手的：在 ofox playground 跑案例、截账单、浏览器授权、qiita/zenn/Reddit 发帖。

---

## 5. skill 仓库操作步骤

### 5.1 前置：向研发要

- 视频接口 `POST /v1/videos` 的 curl 示例，含创建任务和查询任务两步
- 图片接口的 curl 示例
- 参数说明：模型 ID、duration、resolution、aspect_ratio、audio、image 输入、video 输入各自的取值
- 取 key 页面地址
- 一个测试用 key

### 5.2 仓库结构

不新建仓库，放进现有的 `ofoxai/skills`。原因：它已经是按 skills.sh 规范搭好的单仓库，有 CONTRIBUTING.md 定的质量标准、有 skills.sh.json、`npx skills add ofoxai/skills` 已经能装、skills.sh 上已有页面。新建一个平行仓库会把安装量和链接分散到两个地方。

现有结构是每个 skill 一个目录，`SKILL.md` 加 `references/`，脚本也放 references/ 里（cloudflare-drop 就是这样放 .mjs 的）。照这个规矩加：

```
ofoxai/skills/
  README.md                          在 Skills 表里加新行，Group 填 Video / Image
  CONTRIBUTING.md                    已有，新 skill 按它的要求过
  skills.sh.json                     加新 skill 条目
  skills/
    hal-vault/  hal-image/  cloudflare-drop/    已有，不动
    ofox-video-core/                 公共层：调 ofox 视频接口的脚本和参数表，其他场景 skill 引用它
      SKILL.md
      references/ofox-video.sh       创建任务、轮询、下载、报费用
      references/api-params.md
      references/pricing.md
    seedance-short-drama/SKILL.md    场景 skill，每个一个目录
    seedance-anime-drama/SKILL.md
    seedance-ad-creative/SKILL.md
    seedance-product-video/SKILL.md
    ...
```

skill 目录名带模型前缀，`seedance-short-drama` 这种，skills.sh 和 clawhub 上搜 seedance 能命中。

npm 那条路另开一个小仓库 `ofoxai/ofox-media-skills-cli`，只放 bin/cli.js 和 package.json，安装时从 ofoxai/skills 拉对应目录。这样 npm 有包名可发，代码不重复。也可以先不做 npm，前三个目录不依赖它。

给 AI 读的 `llms-install.md` 和 clawhub 用的 `_meta.json` 放各 skill 目录里。

### 5.3 SKILL.md 怎么写

去看 `github.com/EvoLinkAI/gpt-image-2-gen-skill/blob/main/SKILL.md`，结构照它，文字全部重写。必须有的部分：

1. frontmatter：name、description（写清楚场景关键词，agent 靠这个决定要不要触发）、metadata 里声明依赖 curl 和 jq、环境变量 `OFOX_API_KEY`
2. 什么时候触发：列这个场景用户会说的话，参考第 3.2 节最后一列
3. 装完后第一句话：检查 `OFOX_API_KEY`，没有就说"需要一个 Ofox API key，注册 30 秒，要我带你走一遍吗"，然后停。不要列功能菜单。
4. 流程：怎么问用户拿到缺的信息（时长、比例、有没有参考图），怎么拼参数，怎么调 scripts/ofox-video.sh
5. 这个场景的提示词模板和技巧，比如短剧场景要写清楚角色外貌、对白用引号、镜头切换用时间码
6. 参数推荐和费用估算：短剧推荐 15s 720p 约 $3.6，让 agent 生成前告诉用户
7. 常见失败：400 参数错误怎么看、审核拒绝怎么改提示词、任务超时怎么办

### 5.4 脚本

`scripts/ofox-video.sh` 用 bash + curl + jq，不引入其他依赖。功能：接参数、POST 创建任务、按返回的 task id 轮询直到完成、下载 mp4 到当前目录、打印文件路径和本次费用。参数校验放在脚本里，不合法的组合在提交前报错。

### 5.5 测试

每个场景 skill 都要：
1. 在自己机器上用 `npx` 安装到 Claude Code
2. 用自然语言触发一次，生成一条真实视频
3. 故意给错参数一次，确认报错清楚
4. 换一台没装过 jq 的机器再装一次，确认安装提示正确
5. 至少在 Claude Code、Codex CLI、OpenCode 三个 agent 里各跑通一次。Codex 和 OpenCode 只读 frontmatter 的 name 和 description，不认 metadata 里的声明，所以 SKILL.md 正文必须自己写清楚要设 OFOX_API_KEY、要有 curl 和 jq，不能只靠 metadata

没过测试的 skill 不进主分支。

README 的安装一节按 agent 分开给命令：

```
npx skills add ofoxai/skills@seedance-short-drama --agent claude-code
npx skills add ofoxai/skills@seedance-short-drama --agent codex
npx skills add ofoxai/skills@seedance-short-drama --agent opencode
npx skills add ofoxai/skills@seedance-short-drama --agent '*'    # 装给机器上所有 agent
```

并加一句：已经用 ofox key 跑 Codex / Claude Code / Cline 的用户，装这个 skill 不需要新 key，同一个 key 直接出视频。

### 5.6 发布到四个目录：各自的门槛

四个目录都不需要星数、不需要人工审核通过才能上。门槛是格式对、脚本能跑。按难度从低到高：

**npm（最容易，半小时）**
- 要什么：package.json 里 name 唯一、version、bin 指向安装脚本、files 列出要打包的文件。
- 怎么上：npmjs.com 注册，本地 `npm login`，`npm publish`。发完立刻能 `npx ofox-media-skills`。
- 没有审核。唯一会卡的是包名被占，换一个。
- 链接价值：npm 包页 DR 91，会自动出现在 npm.io 等镜像站（Evolink 从 npm.io 拿了 3 条）。

**skills.sh（自动收录页面，但进搜索要靶安装量）**
- 没有提交入口。它不爬 GitHub，也不认 topic。
- 页面层：仓库推到 GitHub 后几分钟内 `skills.sh/ofoxai/ofox-media-skills` 这个页面就自动存在，这就是我们要的外链。
- 搜索和排行层：只有当有人真的跑了 `npx skills add ofoxai/ofox-media-skills`，CLI 发遥测回去，累计到一个没公开的安装量阈值才进搜索。
- 所以"上 skills.sh"分两步：推仓库（自动）；把 `npx skills add ofoxai/ofox-media-skills` 这条命令写进 ofox docs、博客、README、Reddit 帖子，让人去装。前 20 次安装自己团队每台机器装一遍也算。
- 仓库根放一个 `skills.sh.json`，多个 skill 会在页面上归为一组，照 evolink-agent-skills 里那个文件抄格式。

**LobeHub（提交或等抓取，一天内）**
- 它的 skill 标识是 `owner-repo` 格式，条目里带 GitHub 星数和 isValidated 字段，是从 GitHub 抓的。
- 保险做法：lobehub.com 登录，Community → Skills → Import，填仓库里 skill 目录的 GitHub URL，一个场景一条。
- 要什么：目录里有 SKILL.md，frontmatter 有 name 和 description。
- 链接价值：每个 skill 一个页面（Evolink 拿了 5 条），DR 74。

**ClawHub（最严，有自动安全扫描，半天到一天）**
- 用 CLI：`npx clawhub login`，然后 `clawhub skill publish ./skills/short-drama --owner ofoxai --categories media --topics "seedance,video,short-drama"`。一个场景一条命令。
- 发布时会跑自动扫描（SkillSpector、VirusTotal、风险分析），扫不过不上架。扫描主要查这几条：
  - 脚本里用到的每个环境变量、每个命令行工具，frontmatter 的 `metadata.openclaw.requires` 里都声明了。脚本用了 `OFOX_API_KEY`、curl、jq，frontmatter 就必须写 `requires.env: [OFOX_API_KEY]`、`requires.bins: [curl, jq]`。少一个就标"元数据不匹配"。
  - frontmatter 有 `homepage`，指向仓库地址。没有会降信任分。
  - 有 LICENSE 文件。
  - 脚本里不能有 `curl ... | bash` 配 base64 这种写法，直接拒。
  - 只收文本文件，总包 50MB 以内。banner 图这类放仓库可以，但发布目录里别放。
  - name 和文件夹名一致，description 不超过 1024 字符并包含触发词。
- 用 `--dry-run` 先跑一遍看报错，再正式发。
- 多个 skill 的仓库可以用它的 GitHub Action `openclaw/clawhub/.github/workflows/skill-publish.yml`，合并到 main 自动发所有变动的 skill，token 放 GitHub Secrets。
- 链接价值：DR 83，Evolink 从这里拿了 18 条，四个里最多。clawskills.sh、clawbot.ai 会自动镜像 clawhub 的收录，不用另外提交。

**做到什么程度可以开始发**

一个场景 skill 满足下面这些就能同时上四个目录：
1. `skills/<场景>/SKILL.md`，frontmatter 有 name（等于目录名）、description（含触发词）、metadata.openclaw 里声明 OFOX_API_KEY、curl、jq、homepage
2. `scripts/ofox-video.sh` 能在干净机器上跑通一次真实生成
3. 仓库根有 README.md、LICENSE、package.json、skills.sh.json
4. `clawhub skill publish --dry-run` 没报错

不需要：星数、多语言 README（可以后补）、banner 图、showcase。这些是为了好看和转载，不是上架条件。

顺序：npm → 推 GitHub（skills.sh 页面自动生成）→ LobeHub 导入 → ClawHub 发布。第一个场景走完这四步，后面的场景只剩 clawhub 一条命令和 lobehub 一次导入。

### 5.7 节奏

第 1 周：仓库骨架、脚本、短剧和创意广告两个 skill 跑通，发 npm。
第 2 周：漫剧、电商产品视频上线，四个目录全部提交。
之后：每周加两个场景。

### 5.8 Claude Code 操作方法

1. 拿到研发给的 curl 和参数表后，clone `ofoxai/skills`，进 Claude Code，说：
   "读一遍这个仓库的 CONTRIBUTING.md 和 skills/cloudflare-drop 的结构。参考 github.com/EvoLinkAI/gpt-image-2-gen-skill 的 SKILL.md 写法（文字全部重写），在 skills/ 下建 ofox-video-core：SKILL.md 加 references/ofox-video.sh，脚本用 bash + curl + jq 调这个接口（贴 curl），支持创建任务、轮询、下载、打印费用，环境变量 OFOX_API_KEY。frontmatter 的 metadata.openclaw 里声明 OFOX_API_KEY、curl、jq、homepage。"
2. 说："再建 seedance-short-drama，SKILL.md 按短剧场景写：触发词（贴 §3.2 那行）、装完第一句话是检查 key、缺 key 就引导去 ofox 注册、提示词模板、推荐参数和费用估算、常见失败。执行调用 ofox-video-core 的脚本。"
3. 测试。说："把 ofox-video-core 和 seedance-short-drama 装到本地 ~/.claude/skills，用 OFOX_API_KEY=xxx 生成一条 5 秒视频，再故意传一个不支持的分辨率看报错。" 跑通再继续。
4. 说："更新 README.md 的 Skills 表和 skills.sh.json，加这两个 skill。"
5. 说："开分支，提 PR 到 ofoxai/skills。" 走 review 合并。
6. 发目录。说："用 clawhub CLI 对 skills/seedance-short-drama 跑一次 --dry-run，把报错修掉，然后正式 publish，owner 是 ofoxai。" 第一次要 `clawhub login`。LobeHub 在网页上导入，把 skill 目录的 GitHub URL 粘进去。
7. 之后每加一个场景，重复 2 到 6，公共层不用再动。

自己动手的：clawhub、npm 账号注册和登录授权，LobeHub 网页导入，PR review。

---

## 6. 链接规范

所有回链统一带 utm：

```
?utm_source=github&utm_medium={banner|case|badge|readme}&utm_campaign={仓库名}
```

链接目标优先级：
1. 案例 → 站内 prompts 页对应锚点
2. 模型名 → 模型页 `ofox.ai/models/bytedance/seedance-2.5`
3. "Get API Key" → 取 key 页
4. 接口说明 → docs 对应页
5. 首页只在组织 README 里链一次

不要所有链接都指首页。首页已经排第一了，多一条没用；模型页和 prompts 页现在一条 GitHub 链接都没有。

---

## 7. 验收

两周后看：

- 合集仓库：≥80 个案例，≥40 个带账单；三语 README；站内页上线且与仓库同步
- skill 仓库：≥4 个场景 skill 通过第 5.5 节测试；npm 可 `npx` 安装；四个目录提交完成并至少两个已收录
- 链接：Ahrefs 里 ofox 模型页和 prompts 页出现来自 github.com 的链接
- 流量：GA 里 utm_source=github 有数据

一个月后看：GitHub 来源的注册数、四个目录各给了几条链接、仓库星数和 fork 数。

---

## 8. 多人共建怎么安排

skill 仓库天然适合多人做：每个场景一个目录，互不影响。分工按能力来，不按"技术/非技术"来。

| 角色 | 负责什么 | 需要会什么 |
|---|---|---|
| 实习生 | 仓库骨架、scripts/ 两个脚本、bin/cli.js 安装器、GitHub Action、四个目录的账号和首次发布 | 会写 shell 和 node，会 git |
| 内容同事（Chris / marvin） | 各场景 SKILL.md 里的提示词模板、参数推荐、失败修法；awesome 仓库的案例 | 会用 playground，会写 markdown，理解 Seedance 提示词怎么写 |
| 设计同事 | banner、showcase GIF、案例预览图 | 不需要碰代码，把图交给实习生放进 assets/ |
| 研发 | 接口变更时同步 references/api-params.md | 只在接口变时介入 |

SKILL.md 本质是一份给 AI 读的操作说明加提示词模板，没有代码，内容同事直接写比实习生写得好。实习生负责把它接到脚本上并测试。

**仓库设置**

1. 都放在 ofoxai 组织下，不放个人号。实习生加为 ofoxai/skills 和 awesome-seedance-2.5 两个仓库的 admin。
2. main 分支开保护：必须走 PR，至少一人 review，CI 通过才能合并。
3. `.github/CODEOWNERS`：`scripts/`、`bin/`、`.github/` 归实习生；`skills/*/SKILL.md` 归内容同事；`assets/` 归设计。改到谁的目录自动请谁 review。
4. `CONTRIBUTING.md`：写清楚新加一个场景要交哪几个文件、SKILL.md 模板、测试怎么跑。这份也是给外部贡献者看的。
5. `skills/_template/SKILL.md`：新场景从这个复制。
6. CI 两条：`scripts/validate-skills.mjs`（照 evolink-agent-skills 里那个写，检查每个 SKILL.md 的 frontmatter 字段齐、name 等于目录名、声明的 env 和 bins 与脚本一致）；合并到 main 后自动跑 clawhub 的 skill-publish.yml 和 `npm publish`，token 放 GitHub Secrets。这样任何人合并了 PR，四个目录里三个自动更新，只有 LobeHub 要手动导入一次新场景。

**发布账号**

npm、clawhub 的账号用 ofox 公司邮箱注册，token 放仓库 Secrets，不绑任何人的个人号。实习生离开时不用迁移。

**awesome 仓库的共建**

案例靠 PR 进来，这是它能持续涨的方式。CONTRIBUTING.md 里写清楚案例格式（第 4.4 节），要求必须有完整提示词和输出预览。内部同事跑出来的案例也走 PR，方便统一格式。外部贡献者的 PR 由内容同事 review，看提示词是否完整、有没有版权和敏感内容，实习生不用管。

## 9. 需要的账号和权限

- GitHub 组织 ofoxai 下 skills、awesome-seedance-2.5、.github 三个仓库的写权限（组织现有 owner 加人）
- npm 账号（实习生自己注册，包发布后把 ofox 官方账号加为 maintainer）
- ofox 测试 key 和后台账单查看权限
- clawhub、skills.sh、lobehub 用 GitHub 账号登录即可

有问题先看 EvoLinkAI 那几个仓库的源码，再问研发。
